import { Spinnaker } from '@next/font/google';

const spinnaker = Spinnaker({
  weight: '400',
  subsets: ['latin'],
})

export default function AbsoluteBeginners() {
    return (
        <div id="canvasAbsoluteBeginners" className="w-36 h-36"></div>
    );
}